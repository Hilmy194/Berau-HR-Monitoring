# Copy this file OUTSIDE the repository (for example C:\secure\bq-hr-sync.env)
# and replace its values. The service-account JSON must also stay outside Git.
BQ_SERVICE_ACCOUNT_FILE="C:\secure\sa_hris-beraucoal_JSON KEY.json"
BQ_PROJECT_ID="hris-292403"
BQ_DATASET="dw_beraucoal"
BQ_TABLE_P_EMPS="p_emps"
BQ_TABLE_P_TALENT_PROFILE="p_talent_profile"
BQ_TABLE_P_CAREER_HISTORY="p_career_history"
BQ_EXPORT_FILE="C:\secure-exports\bq-hr.json"
# Destination for the raw full-dataset mirror. Keep the URL only in the
# secret-safe config copy outside the repository, never in this example.
# BQ_RAW_DATABASE_URL="postgresql://USER:PASSWORD@localhost:5433/hr_monitoring_bq?schema=public"

# HSE CT raw API export. The collection and output stay outside the repository.
# HSECT_POSTMAN_COLLECTION_FILE="C:\secure\HSE SID Document.postman_collection.json"
# HSECT_RAW_EXPORT_FILE="C:\secure-exports\hsect-raw.json"
# HSECT_EXPORT_FILE="C:\secure-exports\hsect-normalized.json"
# HSECT_COMPANY_IDS="5194" # optional; defaults to companyId in the get employee collection URL
# HSECT_API_KEY="" # optional when x-api-key is already in the collection
# HSECT_TOKEN="" # optional when bearer token is not already in the collection/environment
